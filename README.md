# SparseSet

SparseSet, SparseMap and SparseTree implementation in C++20. Header only. No dependencies.

## Tests

```
apt install meson libgtest-dev libbenchmark-dev
meson setup build
meson test -C build
```

Conan can be used if system dependencies are old. See: `configure.sh`
